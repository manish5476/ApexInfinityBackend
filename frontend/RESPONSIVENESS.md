# Responsiveness

Global layout uses responsive tokens: `--page-gutter`, `--section-gap`, `--content-max-width`, control sizing, and sidebar/header dimensions. The shell becomes a stacked mobile layout below 48rem. New data views must supply a compact card/list representation on mobile instead of compressing a desktop table.
